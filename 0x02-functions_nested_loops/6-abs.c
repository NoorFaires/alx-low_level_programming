#include "main.h"

/**
 * _abs -  a function that computes the absolute value of an integer
 * @n: input number
 * Return: Always 0 (success)
*/

int _abs(int n)
{
	if (n <= 0)
		printf(n);
	else
		printf(-n);
return (0);
}
